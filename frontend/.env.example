# Copy to .env for local overrides.

# Base URL of the API. Leave empty when the API serves the SPA from the same
# origin (single-process production deploy) or when using the Vite dev proxy.
# For a separately-hosted API, set e.g. VITE_API_URL=https://api.example.com
VITE_API_URL=
